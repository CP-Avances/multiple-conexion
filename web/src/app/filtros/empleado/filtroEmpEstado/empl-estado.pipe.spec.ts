import { EmplEstadoPipe } from './empl-estado.pipe';

describe('EmplEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new EmplEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
